# Hyperion E2E Test — nodeos configuration
# Optimized for fast block production and full history capture

# Producer
plugin = eosio::producer_plugin
plugin = eosio::producer_api_plugin
producer-name = eosio
enable-stale-production = true

# Chain
plugin = eosio::chain_plugin
plugin = eosio::chain_api_plugin
chain-state-db-size-mb = 8192
abi-serializer-max-time-ms = 2000
max-transaction-time = 500
contracts-console = true

# State History Plugin (SHiP)
plugin = eosio::state_history_plugin
state-history-endpoint = 0.0.0.0:8080
trace-history = true
chain-state-history = true

# HTTP
plugin = eosio::http_plugin
http-server-address = 0.0.0.0:8888
http-validate-host = false
access-control-allow-origin = *
http-max-response-time-ms = 5000

# Net (disabled — single node dev chain)
# plugin = eosio::net_plugin

